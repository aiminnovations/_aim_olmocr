# Lightweight dependencies for Cloud Run (Parasail API mode)
# No GPU/vLLM dependencies - uses Parasail API instead

# Core olmocr (without GPU extras)
olmocr

# Google Cloud
google-cloud-storage>=2.14.0
google-cloud-firestore>=2.14.0
google-cloud-pubsub>=2.19.0

# HTTP client for Parasail API
aiohttp>=3.9.0

# HTTP server for Cloud Run
fastapi>=0.109.0
uvicorn>=0.27.0

# Utilities
structlog>=24.1.0
python-dateutil>=2.8.2

# PDF processing
pypdf>=3.17.0
pdf2image>=1.16.0
Pillow>=10.0.0
